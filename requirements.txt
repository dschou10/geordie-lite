# Agents
langgraph>=0.2.0
langchain-anthropic>=0.2.0
tavily-python>=0.7.0

# API
fastapi>=0.115.0
uvicorn>=0.30.0

# UI
# HTMX is served via CDN, no package needed

# Database
# sqlite3 is in Python stdlib, no package needed

# Testing
pytest>=8.0.0
